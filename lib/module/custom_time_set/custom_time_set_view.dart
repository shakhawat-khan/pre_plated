import 'package:flutter/material.dart';

class CustomTimeSetView extends StatelessWidget {
  const CustomTimeSetView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}